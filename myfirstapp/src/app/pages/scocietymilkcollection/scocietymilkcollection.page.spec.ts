import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ScocietymilkcollectionPage } from './scocietymilkcollection.page';

describe('ScocietymilkcollectionPage', () => {
  let component: ScocietymilkcollectionPage;
  let fixture: ComponentFixture<ScocietymilkcollectionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ScocietymilkcollectionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
